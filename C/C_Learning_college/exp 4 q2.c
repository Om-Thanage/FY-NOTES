/*#include <stdio.h>

int modify(int arr[], int n) {
    for (int i = 0; i < n; i++) {
        arr[i] *= 3;
    }
}

int main() {
    int arr[10];
    printf("Enter the numbers : \n");
    for (int i = 0; i < 10; i++) {
        scanf("%d",&arr[i]);
    }

    modify(arr, 10);

    printf("Modified array: ");
    for (int i = 0; i < 10; i++) {
        printf("%d ", arr[i]);
    }

    printf("\n");

    return 0;
}
*/
